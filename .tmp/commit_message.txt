feat: Добавлена функция копирования проекта

Этот коммит добавляет возможность создавать копию проекта в генераторе списков.

- Добавлена кнопка "копировать" в элемент списка проектов.
- Реализована функция `copyProject` в `ListGeneratorProjectListViewModel` для создания копии проекта с новым именем.
- Новый проект создается с тем же содержимым, что и исходный, а к имени добавляется "(копия)".